// To present truth values, the keywords `true` and `false` are used.
// Such keywords are expressions having a non-numeric type, named "Boolean".
fn main(){
    let truth = true;
    let falsity = false;
    print!("{} {}", truth, falsity);
}